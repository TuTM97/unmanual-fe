// Define a type for the slice state
export interface ILayoutState {
  rtlData: boolean;
  topMenu: boolean;
  mode: string;
  loading: boolean;
  rtlLoading: boolean;
  menuLoading: boolean;
  mainContentLoading: boolean;
  error: any;
}
